# Feature: [Feature Name]

> Template — copy this file to `features/<feature-name>.md` (kebab-case) the first time you
> actively work on that feature with AI assistance, then delete this blockquote and the
> instructional notes below (`> TEMPLATE:` lines) as you fill each section in.
>
> Facts here must be **verified against live source** — never invented from the `architecture.md`
> reference alone (that's a snapshot, not ground truth). State the verification date; update it
> whenever you re-verify.

---

## Purpose

> TEMPLATE: One paragraph — what this feature does, who/what it serves, why it exists. Not an
> implementation description yet, just the "what and why."

---

## Current State   (verified against live source YYYY-MM-DD)

> TEMPLATE: The concrete files/classes/modules involved, what each owns, and what they
> currently do. Prefer file/class names over line numbers (line numbers rot, see `SDLC.md`).

---

## Invariants   (do not relitigate)

> TEMPLATE: Settled decisions that were paid for once already — don't re-derive or re-suggest a
> rejected alternative. Only add something here once it's actually settled, not on first draft.

---

## Architecture

> TEMPLATE: How the pieces fit together for this feature specifically — data flow, which layer
> owns what (see `architecture.md` for the app-wide layers this slots into). A small diagram
> (mermaid or ASCII) is welcome if it clarifies more than prose would.

---

## Key Paths

> TEMPLATE: A short bullet list of the actual files this feature lives in, so the next session
> can jump straight there.

---

## What Didn't Work

> TEMPLATE: Keep this section forever once it has entries — it's the guardrail against
> re-attempting known dead ends. If nothing has been tried and rejected yet, leave the line
> below as-is rather than inventing history.

No historical dead-ends recorded yet for this feature.

---

## Open Items

> TEMPLATE: Known gaps, TODOs, or unconfirmed claims — mark anything unverified as
> `(unverified)` rather than asserting it.

---

## Verification log

> TEMPLATE: One entry per verification pass — date, what was checked, what was confirmed vs.
> what's still `(unverified)`. This is what lets a future session trust (or distrust) the
> sections above at a glance.

**Verified against live source YYYY-MM-DD:**
- ...
