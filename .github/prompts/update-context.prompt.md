---
description: "Refresh AI-Context (architecture.md, SDLC.md, INDEX.md, feature files) so it matches the current code, after finishing or changing a feature."
agent: "agent"
---
Follow the full ritual in [update-context.md](../../AI-Context/prompts/update-context.md) exactly,
including its scope guard: only `AI-Context/**`, `CLAUDE.md`, and `.github/copilot-instructions.md`
may be edited by this prompt — never AppX Manager's own app source.
